{
  "name": "sofabet",
  "version": "0.1.0",
  "private": true,
  "description": "Football match-outcome probabilities from a Dixon-Coles model on Cloudflare Workers + D1",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local --port 8787",
    "types": "wrangler types",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^3.0.0",
    "wrangler": "^4.0.0"
  }
}
